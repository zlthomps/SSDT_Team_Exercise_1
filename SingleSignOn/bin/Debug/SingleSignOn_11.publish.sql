/*
Deployment script for SingleSignOn

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "SingleSignOn"
:setvar DefaultFilePrefix "SingleSignOn"
:setvar DefaultDataPath "C:\Program Files (x86)\Microsoft SQL Server\MSSQL11.MSSQLSERVER\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files (x86)\Microsoft SQL Server\MSSQL11.MSSQLSERVER\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Altering [dbo].[usp_InsertLogin]...';


GO
ALTER PROCEDURE dbo.usp_InsertLogin
    @User NVARCHAR(50) ,
    @Password NVARCHAR(50) ,
    @RoleName NVARCHAR(50) ,
    @LastLogonDate DATE ,
    @CreatedDate DATE
AS
    DECLARE @RoleId UNIQUEIDENTIFIER
    SET @RoleId = ( SELECT TOP 1 Id
                    FROM    dbo.Role
                    WHERE   Name = @RoleName
                  )

    INSERT  INTO [User]
            ( Username ,
              [Password] ,
              RoleId ,
              LastLogonDate ,
              CreatedDate
            )
    VALUES  ( @User ,
              @Password ,
              @RoleId ,
              @LastLogonDate ,
              @CreatedDate
            )
GO
PRINT N'Altering [dbo].[usp_InsertRole]...';


GO
ALTER PROCEDURE [dbo].[usp_InsertRole]
@Name NVARCHAR(50),@CreatedDate DATE, @ModifiedDate DATE
AS
	INSERT INTO [Role] (Name, CreatedDate, ModifiedDate)
	VALUES(@Name, @CreatedDate, @ModifiedDate)
GO
PRINT N'Altering [dbo].[usp_UpdateLogin]...';


GO
ALTER PROCEDURE [dbo].[usp_UpdateLogin]
@Username NVARCHAR(50), @NewPassword NVARCHAR(50)

AS

UPDATE [dbo].[User] 
SET [Password] = @NewPassword
WHERE [Username] = @Username
GO
PRINT N'Altering [dbo].[usp_UpdateRole]...';


GO
ALTER PROCEDURE [dbo].[usp_UpdateRole]
	@ExistingName NVARCHAR(50),
	@NewName NVARCHAR(50)
AS
	UPDATE [Role] SET Name = @NewName
	WHERE Name = @ExistingName
GO
PRINT N'Update complete.';


GO
